package msrp.test.stack;

import junit.framework.*;

public class MsrpListnerTest
    extends TestCase {
  public void testListner() {
    /*
         MsrpListner listen = new MsrpListner();
         Thread th = new Thread(listen);
         th.start();

         MessageFactoryImpl messageFactory = new MessageFactoryImpl();
         ToPath to = new ToPath("msrp://inam@127.0.0.1:6060/werty;tcp");
         FromPath from = new FromPath("msrp://inam@127.0.0.1:6061/werty;tcp");
         MessageID msgID = new MessageID("5776");
         ByteRange range = new ByteRange(5, 5, 5);
         ContentType contentType = new ContentType("plain/text");
         String body = "inam";
     MsrpRequest req = messageFactory.createRequest("SEND", to, from, msgID,
        range, contentType, body);
         TCPChannel chan = new TCPChannel(null);
         chan.sendRequest(req);
     */
  }
}
